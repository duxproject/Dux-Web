export interface Package {
    id: string;
    firstName: string;
    lastName: string;
    ownerId: string;
    packageName: string;
    duration: string;
    description: string;
    price: string;
 }
